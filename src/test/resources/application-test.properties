spring.application.name=expensestracker-test

# Test Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/expensestracker?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456789
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Disable SQL logging in tests for cleaner output
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
logging.level.org.springframework.data.jpa.repository.config=WARN

prefix=api

# Test Mail Configuration (use fake SMTP or disable)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=test-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Test JWT Configuration
jwt.secretKey=TaqlmGv1iEDMRiFp/pHuID1+T84IABfuA0xXh4GhiUI=
jwt.expiration=1200000
jwt.refresh-expiration=1296000000

# Disable SSL for tests
server.ssl.enabled=false
server.port=0

# Test Encryption Keys
app.security.db-encryption-key=9fA3kL2M7QeR8ZxC1B5U6YpH0sT4WJVa
app.security.db-encryption-iv=K8s2QZ1L0x7M9BVa

# Security Settings
app.security.login.max-attempts=5
app.security.login.lock-duration-minutes=15

# Rate Limiting
app.rate-limit.capacity=10
app.rate-limit.refill-tokens=10
app.rate-limit.refill-duration-minutes=1
